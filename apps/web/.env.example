# Database Configuration
# For local development, leave these empty to use file-based database
# For production with Vercel Postgres, these will be auto-populated
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Authentication
NEXTAUTH_URL=https://your-app-name.vercel.app
NEXTAUTH_SECRET=your-secret-key-here

# Application
NODE_ENV=production
APP_URL=https://your-app-name.vercel.app

# Email Configuration
# Option 1: Resend (Recommended - 3000 free emails/month)
# Sign up at https://resend.com/ and get your API key
RESEND_API_KEY=

# Option 2: Traditional SMTP (If you have your own email server)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="PulseCRM <noreply@your-domain.com>"

# Document Signing Recipients
DEFAULT_EMAIL=user@example.com
RECIPIENT_EMAILS=email1@example.com,email2@example.com

# OAuth Integration Credentials
# ================================

# Google (Used for both Gmail and Google Calendar)
# To enable Google integrations:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable APIs: Google Calendar API, Gmail API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URIs:
#    - https://your-app.vercel.app/api/oauth/google-calendar/callback
#    - https://your-app.vercel.app/api/oauth/gmail/callback
#    - http://localhost:3010/api/oauth/google-calendar/callback (for dev)
#    - http://localhost:3010/api/oauth/gmail/callback (for dev)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Legacy support for Google Calendar (optional, uses above if not set)
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
GOOGLE_CALENDAR_REDIRECT_URI=

# Slack Integration
# To enable Slack integration:
# 1. Go to https://api.slack.com/apps
# 2. Create a new app (select "From scratch")
# 3. Add OAuth scopes: channels:read, chat:write, users:read, incoming-webhook
# 4. Add redirect URL: https://your-app.vercel.app/api/oauth/slack/callback
# 5. Install app to workspace
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# QuickBooks Integration
# To enable QuickBooks integration:
# 1. Go to https://developer.intuit.com/
# 2. Create an app (select QuickBooks Online and Payments)
# 3. Add redirect URI: https://your-app.vercel.app/api/oauth/quickbooks/callback
# 4. Note: Use sandbox for development, production keys for live
QUICKBOOKS_CLIENT_ID=
QUICKBOOKS_CLIENT_SECRET=

# Stripe Integration (API Key based, not OAuth)
# To enable Stripe integration:
# 1. Go to https://dashboard.stripe.com/apikeys
# 2. Create restricted API key with necessary permissions
# 3. Permissions needed: Charges (read), Customers (write), Invoices (write)
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional: Analytics
# VERCEL_ANALYTICS_ID=
